import Snippet from '~/components/snippet'
import Caption from '~/components/text/caption'

export const Author = ({ name }) => <span>{name}</span>

With a <Author name={props.name} /> app set up, it is ready to deploy live with [ZEIT Now](/docs/v2).

Using [Now CLI](/download), deploy your app with **a single command**:

<Snippet dark text="now" />
<Caption>Deploying your <Author name={props.name} /> app with Now CLI.</Caption>
